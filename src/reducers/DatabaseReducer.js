import { CREATE_USER, LOGIN_USER, SIGN_OUT_USER } from "../actions/constables";

const DatabaseReducer = (state = {}, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default DatabaseReducer;
